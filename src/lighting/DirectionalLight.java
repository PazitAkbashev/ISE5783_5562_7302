package lighting;

import primitives.Color;
import primitives.Vector;

public class DirectionalLight extends Light  implements  LightSource
    public DirectionalLight(Color sphereLightColor, Vector direction
    }
}
